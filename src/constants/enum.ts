export enum EnumUserVerifyStatus {
  Unverified,
  Verified,
  Banned
}

export enum EnumTokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum EnumMediaType {
  Image,
  Video
}
